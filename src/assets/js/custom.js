'use strict';
import $                             from 'jquery';
import { skipLinkFocus, navigation } from __dirname + '/vendor-modules';
import debug                         from __dirname + '/debug';

/**
 * Main JavaScript Source
 *
 * @since 1.0.0
 */
skipLinkFocus(), navigation();

( function( win ) {

    var doc  = document,
        html = doc.documentElement,
        head = doc.head,
        body = doc.body;

}( this ) );
