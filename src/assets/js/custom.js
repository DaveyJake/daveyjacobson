'use strict';
import $     from 'jquery';
import debug from 'debug';

/**
 * Main JavaScript Source
 *
 * @since 1.0.0
 */
( function( win ) {

    var doc  = document,
        html = doc.documentElement,
        head = doc.head,
        body = doc.body;

    /*(function( $ ) {

        var $win  = $( win ),
            $doc  = $( doc ),
            $html = $( html ),
            $head = $( head ),
            $body = $( body );

    })( jQuery );*/

}( this ) );
